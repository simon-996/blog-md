---
title: 【内网穿透】在Linux中安装frp
tags:
  - frp
  - 内网穿透
  - Linux
categories:
  - 服务器
abbrlink: d68a06f9
date: 2025-06-12 17:48:52
---

> 前置条件：拥有一台具备公网ip的服务器

### 什么是frp

frp 是一个 `Github` 的一个开源软件，在有公网服务器的情况下，本地电脑或服务器可以借助公网服务器，打通隧道，使得本地的服务可以利用公网服务器的 ip 在公网直接访问 [Github 地址](https://github.com/fatedier/frp/releases)

> frp 分为`frpc`和`frps`
>
> frpc 是客户端，在本地电脑使用
>
> frps 是服务端，在有公网的服务器使用

[Github 地址](https://github.com/fatedier/frp/releases)中下载后的压缩包包含客户端和服务端

## frps安装

### 下载

到[Github 地址](https://github.com/fatedier/frp/releases)中下载对应版本的压缩包到本地电脑或服务器（以下按照 Linux服务器安装为标准）

我下载的文件是`frp_0.62.1_linux_amd64.tar.gz`，建议下载到`/opt`目录下。命令如下

```bash
cd /opt
sudo wget https://github.com/fatedier/frp/releases/download/v0.62.1/frp_0.62.1_linux_amd64.tar.gz
```

使用以下命令进行解压

```bash
tar -zxvf `frp_0.62.1_linux_amd64.tar.gz`
```

解压后得到如下文件

![image-20250612180115277](https://simon-picgo-img.oss-cn-chengdu.aliyuncs.com/image-20250612180115277.png)

`frpc` 客户端

`frpc.toml` 客户端配置文件

`frps` 服务端

`frps.toml`服务端配置文件

### 配置

编辑`frps.toml`配置文件，参考如下：

``` bash
bindPort = 7000      		#本地frps运行的端口号
vhostHTTPPort = 8080 		#虚拟端口


auth.token = "xxxxxxx"  #身份验证令牌，frpc要与frps一致


# Server Dashboard，可以查看frp服务状态以及统计信息
webServer.addr = "0.0.0.0" #后台管理地址
webServer.port = 7500 		 #后台管理端口
webServer.user = ""        #后台登录用户名
webServer.password = ""    #后台登录密码
```

### 创建frps systemd服务

创建后支持`frps`开机自启，否则每次重启后需要手动启动`frps`

>  systemd 是 Linux 系统的一种初始化系统实现，它管理的各种服务叫做 **Unit**，每个服务对应一个 **Unit 配置文件**
>
>  在本教程中，frpc 会被注册为一个服务，我们让 **systemd** 来自动管理 frpc，借此实现开机自启和后台运行
>
>  systemd 的服务有两种状态:
>
>  - `运行中`/`已停止`/`错误`
>  - `启用`/`禁用`
>
>  如果 **启用** 了服务，开机的时候服务就会自启，反之就不会

#### 编写配置文件

systemd 的 **Unit 配置文件** 通常位于这些目录中:

- /lib/systemd/system （供软件包使用）
- /etc/systemd/system （供管理员使用）

本教程将选用第二个目录来放置 frpc 的 **Unit 配置文件**，并且 frpc 启用后报错退出时每分钟会自动重启一次

编写`service`文件

```bash
sudo vim /etc/systemd/system/frps.service
```

内容为： （将 `ExecStart` 中的目录改为自己的目录）

```bash
[Unit]
Description=frps service
After=network.target

[Service]
Type=idle
ExecStart=/opt/frp_0.62.1_linux_amd64/frps -c /opt/frp_0.62.1_linux_amd64/frps.toml   
Restart=on-failure
RestartSec=60s
User=root

[Install]
WantedBy=multi-user.target
```

执行下面的命令重载 systemd

```bash
sudo systemctl daemon-reload
```

使用以下命令可以查看是否启动成功

``` bash
sudo systemctl status frps
```

如果成功显示如下

![image-20250613165443747](https://simon-picgo-img.oss-cn-chengdu.aliyuncs.com/image-20250613165443747.png)

如果有问题，修改配置文件后重新启动`frps`

重启

``` bash
sudo systemctl restart frps
```

启动

```bash
sudo systemctl start frps
```

停止

```bash
sudo systemctl stop frps
```

## frpc安装

### 下载

到[Github 地址](https://github.com/fatedier/frp/releases)中下载对应版本的压缩包到本地电脑或服务器（以下按照 Linux服务器安装为标准）

我下载的文件是`frp_0.62.1_linux_amd64.tar.gz`，建议下载到`/opt`目录下。命令如下

```bash
cd /opt
sudo wget https://github.com/fatedier/frp/releases/download/v0.62.1/frp_0.62.1_linux_amd64.tar.gz
```

使用以下命令进行解压

```bash
tar -zxvf `frp_0.62.1_linux_amd64.tar.gz`
```

解压后得到如下文件

![image-20250612180115277](https://simon-picgo-img.oss-cn-chengdu.aliyuncs.com/image-20250612180115277.png)

`frpc` 客户端

`frpc.toml` 客户端配置文件

`frps` 服务端

`frps.toml`服务端配置文件

### 配置

编辑`frpc.toml`配置文件，参考如下：

```bash
serverAddr = ""   #公网服务器的公网ip
serverPort = 7000 #公网服务器的frps端口
auth.token = "" #公网服务器中fprs.toml 配置的token

[[proxies]]
name = "hlj-api"
type = "tcp"
localIP = "127.0.0.1" # 需要暴露的服务的IP
localPort = 9090 # 将本地9000端口的服务暴露在公网的6060端口
remotePort = 6060 # 暴露服务的公网入口

[[proxies]]
name = "hlj-web"
type = "tcp"	
localIP = "127.0.0.1" # 需要暴露的服务的IP
localPort = 9098 # 将本地9098端口的服务暴露在公网的6068端口
remotePort = 6068 # 暴露服务的公网入口

```

每一个`proxies`都代表一个`隧道`， 即本地端口，暴露到公网服务器的端口。

### 创建frpc systemd服务

创建后支持`frpc`开机自启，否则每次重启后需要手动启动`frpc`

#### 编写配置文件

编写`service`文件

```bash
sudo vim /etc/systemd/system/frpc.service
```

内容为： （将 `ExecStart` 中的目录改为自己的目录）

```bash
[Unit]
Description=frpc service
After=network.target

[Service]
Type=idle
ExecStart=/opt/frp_0.62.1_linux_amd64/frpc -c /opt/frp_0.62.1_linux_amd64/frpc.toml   
Restart=on-failure
RestartSec=60s
User=root

[Install]
WantedBy=multi-user.target
```

执行下面的命令重载 systemd

```bash
sudo systemctl daemon-reload
```

使用以下命令可以查看是否启动成功

``` bash
sudo systemctl status frpc
```

如果成功显示如下

![image-20250612184000860](https://simon-picgo-img.oss-cn-chengdu.aliyuncs.com/image-20250612184000860.png)

如果有问题，修改配置文件后重新启动`frpc`

重启

``` bash
sudo systemctl restart frpc
```

启动

```bash
sudo systemctl start frpc
```

停止

```bash
sudo systemctl stop frpc
```

### 附加项

如果`proxies`过多不好维护，可以将配置文件拆分成多份，如以上配置，可以在`frp`安装目录中创建`confd`（自己取名字）目录，然后将所有的`proxies`以项目为单位拆分成多个文件。

![image-20250612181140659](https://simon-picgo-img.oss-cn-chengdu.aliyuncs.com/image-20250612181140659.png)

`hlj.tomal`中的内容为

```bash
[[proxies]]
name = "hlj-api"
type = "tcp"
localIP = "127.0.0.1" # 需要暴露的服务的IP
localPort = 9090 # 将本地9000端口的服务暴露在公网的6060端口
remotePort = 6060 # 暴露服务的公网入口

[[proxies]]
name = "hlj-web"
type = "tcp"
localIP = "127.0.0.1" # 需要暴露的服务的IP
localPort = 9098 # 将本地9098端口的服务暴露在公网的6068端口
remotePort = 6068 # 暴露服务的公网入口
```

修改后的`frpc.toml`为

```bash
serverAddr = ""   	#公网服务器的公网ip
serverPort = 7000 	#公网服务器的frps端口
auth.token = "" 		#公网服务器中fprs.toml 配置的token
includes = ["/opt/frp_0.62.1_linux_amd64/confd/*.toml"]  #引入confd目录下所有的配置文件(路径根据自己的pwd来)
```

