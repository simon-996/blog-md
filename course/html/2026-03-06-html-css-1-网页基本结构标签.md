---
title: html-css-1-网页基本结构标签
tags:
  - 网页设计与制作
  - css
  - html
categories:
  - 课件
abbrlink: 1ec5d0bd
date: 2026-03-06 19:13:29
---

## 🎯 学习目标 

1. 理解 HTML 的基本文档结构。 
2. 掌握双标签与单标签的区别。 
3. 学会使用常用的文本标签（标题、段落、样式）。 
4. **核心理念**：理解什么是“语义化”，并养成良好习惯。

---

## HTML 简介

**HTML** (Hyper text Markup Language) = **超文本标记语言**。 

* **超文本**：不仅仅是文字，还可以包含图片、链接、视频等。 
* **标记语言**：使用**标签（Tags）**来标记内容的含义。 
* **核心符号**：`< >` (尖括号)。	

### HTML 基本骨架 每个 HTML 文件都必须包含以下结构：

``` html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>my first website</title>
</head>
<body>
    <!-- 这里写页面显示的内容 -->
    <h1>hello world！</h1>
</body>
</html>
```

| 标签              | 作用         | 说明                                               |
| ----------------- | ------------ | -------------------------------------------------- |
| `<!DOCTYPE html>` | 声明文档类型 | 必须放在第一行，告诉浏览器这是 HTML5。             |
| `<html>`          | 根元素       | 包裹整个网页，`lang="zh-CN"` 表示中文。            |
| `<head>`          | 头部信息     | 存放不显示在页面上的配置（编码、标题、CSS 链接）。 |
| `<meta>`          | 元数据       | 关键属性：`charset="UTF-8"` (防止乱码)。           |
| `<title>`         | 页面标题     | 显示在浏览器标签页上的文字。                       |
| `<body>`          | 主体内容     | 存放显示在页面上的所有内容（文字、图片等）。       |

💡 **小贴士：注释标签**
在代码中写备注，浏览器不会显示，方便开发者阅读。

```html
<!-- 这是一行注释 -->
<!-- 
  这是
  多行注释
-->
```

## 标签（Tags）**详解**

### 标签的组成

符号：标签用<>尖括号表示

组成：开始标签、内容、结束标签

别称：标签也称为` 元素`

### 标签的分类

大部分标签都是双标签，少部分单标签

### 双标签 **(Paired Tags)**

有开始也有结束，内容写在中间。**绝大多数标签都是双标签。**

``` html
<h1>一级标题</h1>
<p>这是一个段落</p>
```

### **单标签 (Self-Closing Tags)**

只有一个标签，没有结束标签，也不能包含内容。

```html
<hr />  <!-- 水平线 -->
<br />  <!-- 换行 -->
<img src="logo.png" /> <!-- 图片 -->
```

> ⚠️ 注意：在 HTML5 中，末尾的 `/` 可以省略（如 `<br>`），但写上也是合法的。

## 标签的关系

### **并列关系**：两个标签互不包含，平级排列。

```html
<h1>一级标题</h1>
<h2>二级标题</h2>
```

### **嵌套关系**：一个标签包含另一个标签（子标签必须在父标签内部闭合）。

```html
<div>
	<h1>一级标题</h1>
</div>
```

## 常用文字标签

### **段落与标题**

| 标签      | 英文全称  | 作用     | 特点                                                      |
| --------- | --------- | -------- | --------------------------------------------------------- |
| `<p>`     | Paragraph | 定义段落 | 独占一行，上下有默认间距。                                |
| `h1`~`h6` | Heading   | 定义标题 | `h1` 最大（最重要），`h6` 最小。一个页面建议只有一个 h1。 |

```html
<p>段落</p>
<h1>一级标题</h1>
<h2>二级标题</h2>
<h3>三级标题</h3>
<h4>四级标题</h4>
<h5>五级标题</h5>
<h6>六级标题</h6>
```

### **字体样式标签 (重点：语义化)**

HTML 不仅是为了“好看”，更是为了表达“含义”。

| 标签            | 作用       | 语义说明                             | 推荐度     |
| --------------- | ---------- | ------------------------------------ | ---------- |
| `<b>`           | 加粗       | 纯样式加粗，无特殊含义               | ⭐⭐         |
| `<strong>`      | 加粗       | 表示“重要/强调”，搜索引擎会重视      | ⭐⭐⭐⭐⭐      |
| `<i>`           | 斜体       | 纯样式斜体，无特殊含义               | ⭐⭐         |
| `<em>`          | 斜体       | 表示“语气强调”                       | ⭐⭐⭐⭐⭐      |
| `<u>`           | 下划线     | 表示专有名词等（慎用，易与链接混淆） | ⭐⭐         |
| `<s>` / `<del>` | 删除线     | 表示已失效、降价等内容               | ⭐⭐⭐⭐       |
| `<font>`        | 自定义字体 | 已过时！ 请用 CSS 代替               | ❌ 禁止使用 |

#### **代码示例**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>文本标签演示</title>
</head>
<body>
    <h2>1. 强调的重要性</h2>
    <p>普通加粗：<b>这只是看起来粗</b></p>
    <p>语义加粗：<strong>这非常重要！（SEO 友好）</strong></p>

    <h2>2. 价格展示</h2>
    <p>原价：<s>199 元</s> 现价：<strong>99 元</strong></p>

    <h2>3. 过时的 font 标签（仅作了解）</h2>
    <!-- 现代开发中严禁使用 font 标签，请使用 CSS -->
    <p><font color="red" size="5">这是红色的字（不推荐）</font></p>
</body>
</html>
```

## HTML 特殊字符（实体符号）

在 HTML 中，像 `< > & 空格` 等符号有特殊含义，**不能直接书写**，需要使用**字符实体**来显示。

### 常用特殊字符

- ` `  空格（不会换行的空格）
- `<`    小于号 <
- `>`    大于号 >
- `&`   和号 &
- `©`  版权符号 ©

### 代码示例

```html
<p>HTML&nbsp;&nbsp;&nbsp;特殊空格演示</p>
<p>标签写法：&lt;html&gt;</p>
<p>版权所有：&copy; 2026</p>
```

## 列表标签（有序 + 无序）

列表用于整齐排列内容，分为**无序列表**和**有序列表**。

### 无序列表 `ul`

**自带属性：type**（设置项目符号）

- `disc`   默认 实心圆
- `circle` 空心圆
- `square` 实心方块

``` html
<ul type="circle">
  <li>HTML 基础</li>
  <li>CSS 样式</li>
</ul>
```

### 有序列表 `ol`

**自带属性：type、start、reversed**

1. **type**：设置序号类型

   

   - `1`  默认 数字 1 2 3
   - `a`  小写字母 a b c
   - `A`  大写字母 A B C
   - `i`  小写罗马数字 ⅰ ⅱ ⅲ
   - `I`  大写罗马数字 Ⅰ Ⅱ Ⅲ

2. **start**：设置从几开始

3. **reversed**：倒序排列

```html
<ol type="A" start="3">
  <li>打开编辑器</li>
  <li>编写代码</li>
</ol>
```

## 超链接标签 `a`

用于**页面跳转**，是网页最核心功能之一。

### 基本语法

```html
<a href="跳转地址" target="打开方式">链接文字</a>
```

### 属性说明

- `href`：目标地址（网址 / 文件路径）
- `target="_blank"`：新标签页打开

### 示例

``` html
<a href="https://www.baidu.com">百度一下</a>
<a href="https://developer.mozilla.org" target="_blank">MDN 官方文档（新窗口）</a>
```

## 图片标签 `img`

用于在网页中**显示图片**，属于**单标签**。

### 基本语法

```html
<img src="图片路径" alt="图片说明" width="宽度" height="高度">
```

### 属性说明

- `src`：图片地址（必填）
- `alt`：图片加载失败时的文字
- `width/height`：设置尺寸

### 示例

```html
<img src="logo.png" alt="网站LOGO" width="200">
```

> ### 绝对路径 & 相对路径

### 绝对路径（Absolute Path）

**定义**：完整的文件地址，包含完整路径 / 网址。

**两种写法**：

1. **网络绝对路径**：直接使用网络图片 URL

   ```html
   <img src="https://swjtuhc.cn/images/logo_w-87af9f14.png" alt="网络图片">
   ```

2. **本地绝对路径**：电脑完整盘符路径（不推荐）

   ```html
   <img src="C:\web\images\logo.png" alt="本地绝对路径">
   ```

**特点**：

- 路径固定，移植性差
- 项目上线**不推荐**使用本地绝对路径

### 相对路径

### 相对路径（Relative Path）【重点】

**定义**：以当前 HTML 文件为起点，找到图片的位置。

#### 同级目录（图片和 HTML 文件在同一文件夹）

直接写图片名：

```html
<img src="logo.png" alt="同级图片">
```

#### 下级目录（图片在 HTML 文件的子文件夹里）

`文件夹名/图片名`：

```html
<img src="images/logo.png" alt="下级目录图片">
```

#### 上级目录（图片在 HTML 文件的上一级文件夹）

`../` 表示返回上一级：

``` html
<img src="../logo.png" alt="上级目录图片">
```

**特点**：

- 项目开发**最常用**
- 移植性强，适合网站部署

## **核心理念：HTML 语义化**

> **什么是语义化？**
> 根据内容的**架构和含义**选择恰当的 HTML 标签，而不是只为了“长得像”。
> **原则**：让标签回归「含义」，让 CSS 负责「样式」。

### **为什么要语义化？**

1. **SEO (搜索引擎优化)**：搜索引擎（如百度、Google）通过 `h1` 知道文章标题，通过 `p` 知道正文，合理的结构能提升排名。
2. **可读性与维护**：开发者看到 `<h2>` 就知道是大章节，看到 `<nav>` 就知道是导航，代码一目了然。
3. **无障碍访问**：屏幕阅读器（视障人士使用）能准确朗读出“这是标题”、“这是强调内容”。

```html
<!-- ❌ 错误的写法：只用样式标签堆砌 -->
<b style="font-size: 24px;">第一章：HTML 简介</b>
<p style="font-weight: bold;">1.1 什么是 HTML？</p>

<!-- ✅ 正确的写法：使用语义标签 -->
<h1>第一章：HTML 简介</h1>
<h2>1.1 什么是 HTML？</h2>
<p>HTML 是构建网页的核心语言...</p>
<p>核心价值是<strong>语义化</strong>。</p>
```

### **总结**

- **结构**：`<!DOCTYPE>` + `html` + `head` + `body`。
- **标签**：分清双标签和单标签。
- **文本**：标题用 `h1-h6`，段落用 `p`，强调用 `strong/em`。
- **原则**：**拒绝 `<font>`**，坚持**语义化**写作。 
